---
sidebar_position: 2
---

import ResizableLadlePreview from "@site/src/components/ResizableLadlePreview";
import { BlockViewerView } from "@site/src/components/Resizable/preview";
import ResizableLadlePreviewCode from "!!raw-loader!@site/src/components/ResizableLadlePreview?raw";
import HelloWorldStoryCode from "!!raw-loader!@site/src/stories/hello.stories.tsx?raw";
import CodeBlock from "@theme/CodeBlock";
import TailwindConfigCode from "!!raw-loader!../../tailwind.config.ts";
import TailwindCSSCode from "!!raw-loader!@site/src/styles/tailwind.css";

# Resizable Preview

Ladle helps in preview of components. If you are building a component library and you want help showing previews of components, Ladle can help you.

## Configure ShadCN

### Install packages

We are going to use react-resizable-panels and some styling from shadcn

```bash
npm i tailwindcss-animate clsx tailwind-merge react-resizable-panels lucide-react
```

### Add CSS Variables to tailwind.CSS

<CodeBlock title="src/styles/tailwind.css" language="css">
  {TailwindCSSCode}
</CodeBlock>

### Add ShanCN Variables to tailwind.config

<CodeBlock title="tailwind.config.ts" language="ts">
  {TailwindConfigCode}
</CodeBlock>

## Add ResizableLadlePreview.tsx

<CodeBlock title="src/components/ResizableLadlePreview.tsx" language="tsx">
  {ResizableLadlePreviewCode}
</CodeBlock>

## Test Preview

### Add Hello World Story

<CodeBlock title="src/stories/hello.stories.tsx" language="tsx">
  {HelloWorldStoryCode}
</CodeBlock>

### Run Ladle

For following to work during development. Keep both commands running:

```bash
npm run start
```

```bash
npm run ladle:serve
```

### Example Story with Preview

In any .mdx file of docusaurus add following The components loads ladle preview of a story in an iframe which is resizable.

```tsx
import ResizableLadlePreview from "@site/src/components/ResizableLadlePreview";
<ResizableLadlePreview story="hello--world" height={200} />;
```

It should show up like following:

### Unrealistic Example

<ResizableLadlePreview story="hello--world" height={200} />

### Realistic Example

<ResizableLadlePreview story="page--world" height={600} />
